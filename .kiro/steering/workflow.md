# Workflow Rules

- Never run `git push` without explicit user approval. Suggest the command instead and wait for confirmation.
- ALWAYS create feature branches when making changes to the code. Use naming conventions for branch naming (fix/branch-name, spec/..., doc/..., etc) and commit messages (fix: desc, feat: desc, doc: desc, etc.)
